{
  "name": "@microservice/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript type definitions — type-only, no runtime code",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build"
  }
}
